<!-- STEP id="setup" -->
Kabir watches the pipeline jam at step six.

"So the buckets are the bottleneck. Not the sugar, not the coins."

"The buckets. Remember that when you smell the batter in the morning."

**Predict first:** which step jams, and does the pipeline make any more ATP after that?

<!-- STEP id="run" -->
**Turn the dial.** Step six, the one that fills a bucket, needs an empty NAD+. With every bucket full, step six waits. The pieces pile up behind it. Steps seven to ten, where the coins are earned, get nothing. ATP falls to zero. The two coins spent at steps one and three are still spent; the pipeline is now losing coins. The cell's till empties. In the air panel, something off-screen keeps emptying the buckets and the pipeline runs on. In the no-air panel, it jams within seconds.

<!-- STEP id="nails" -->
**What this nails:** Glycolysis cannot run without empty buckets. Whatever empties them decides whether a cell can burn sugar at all. With air, the power plant does it. Without air, the cell must find another way, and the batter on the counter is doing exactly that tonight.
